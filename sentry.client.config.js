module.exports = {
  // Minimal client config to satisfy @sentry/nextjs during dev.
  // If you use Sentry, set SENTRY_DSN in your env and uncomment the dsn below.
  // dsn: process.env.SENTRY_DSN || '',
};